% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{mangle}
\alias{mangle}
\title{Mutate and keep only the new columns}
\usage{
mangle(.data, ...)
}
\arguments{
\item{.data}{A data frame.}

\item{...}{Named expressions, as passed to \code{\link[dplyr:mutate]{dplyr::mutate()}}. Only the
columns named here survive.}
}
\value{
A data frame containing only the columns named in \code{...}, in that
order.
}
\description{
A \code{\link[dplyr:mutate]{dplyr::mutate()}} that discards every column it was not asked to
create, so \code{mangle(d, y = y, x0 = x)} returns a two-column frame named
\code{y} and \code{x0}.
}
\details{
This uses non-standard evaluation to recover the names of \code{...}, so it
cannot be called programmatically with a pre-built list of expressions.
}
\examples{
mangle(data.frame(x = 1:3, y = 4:6), doubled = x * 2)
}
