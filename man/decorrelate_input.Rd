% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorrelate.R
\name{decorrelate_input}
\alias{decorrelate_input}
\title{Flatten the y ~ x slope by duplicating influential points}
\usage{
decorrelate_input(data, max_iter = 100)
}
\arguments{
\item{data}{A data frame with numeric columns \code{x} and \code{y}.}

\item{max_iter}{Maximum number of duplication rounds.}
}
\value{
\code{data} with duplicated rows appended and \code{y} re-centred on zero.
}
\description{
Drives the slope of \code{y ~ x} towards zero without moving any of the
existing points. On each iteration it finds the observation whose
influence on the slope (see \code{\link[stats:influence]{stats::influence()}}) most closely cancels
the slope currently fit, and appends however many copies of that
observation are needed. It stops early once no positive number of
copies would help.
}
\details{
Because rows are only ever appended, the result has at least as many
rows as the input, and usually a few more.
}
\examples{
set.seed(1)
d <- data.frame(x = rnorm(50), y = rnorm(50))
nrow(decorrelate_input(d))
}
