% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stegapography.R
\name{stegapography}
\alias{stegapography}
\title{Hide a picture in a regression's residuals}
\usage{
stegapography(data, n_x = 5, max_iter = 100)
}
\arguments{
\item{data}{A data frame with numeric columns \code{x} and \code{y}, holding the
picture to hide.}

\item{n_x}{Number of \code{x} columns to produce.}

\item{max_iter}{Passed to \code{\link[=decorrelate_input]{decorrelate_input()}}.}
}
\value{
A data frame with \code{n_x + 1} columns named \code{y}, \code{x0}, \code{x1}, ...,
\verb{x\{n_x - 1\}}. It has \emph{more rows than \code{data}}, because
\code{\link[=decorrelate_input]{decorrelate_input()}} flattens the input slope by duplicating points.
}
\description{
Takes a two-column picture and expands it into \code{n_x + 1} columns that
look like unremarkable predictors: the point cloud is decorrelated, the
picture is folded into \code{y}, noise columns are added, and the whole thing
is rotated so the signal is smeared across every \code{x} column rather than
sitting in one of them.
}
\details{
The picture is recovered by regressing \code{y} on the \code{x} columns and
plotting the residuals.
}
\examples{
set.seed(1)
picture <- data.frame(x = rnorm(50), y = rnorm(50))
hidden <- stegapography(picture)
round(cor(hidden), 3)
plot(resid(lm(y ~ ., hidden)) ~ fitted(lm(y ~ ., hidden)))
}
